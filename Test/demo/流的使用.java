package ruins.demo;

import java.util.*;
import java.util.stream.Collectors;
import java.util.stream.Stream;

/**
 * @author defang.yan
 * @date 19-4-23 下午2:47
 */
public class 流的使用 {
    //流  主要分三步   创建流，中间操作和最终操作
    public static void main(String[] args) {
//        String s[] = {"aa","bb","cc","dd","ee","bb"};
//        ArrayList<String> arrayList = new ArrayList<String>(Arrays.asList(s));
//        Map<String,String> map = new HashMap<>();
//        Stream<String> stream = arrayList.stream();
//        System.out.println(stream);//创建流  也就是常见的.stream()
//
//
//        //中间操作  常见的有 filter map limit sorted distinct
//        List<String> bb = stream.filter(s1 -> s1.equals("bb")).collect(Collectors.toList());
//        map.forEach((s1, s2) -> System.out.println());
//        bb.forEach(System.out::println);
//        //最终操作  用的 较多的是collect 起来
        System.out.println(11);

    }
}
